2)
	a) scanf( "d", &valor );

	b) printf( "O produto de %d e %d é %d"\n, x, y, z );

	c) somaDosNúmeros = primeiroNúmero + segundoNúmero 

	d) if ( a >= b) {
		a >= b;
	   }

	e) scanf( "%d", &umInteiro );

	f) printf( "Modulo de %d dividido por %d e %d\n", x, y, x / y );

	g) if ( x == y ) {
		printf( "%d é igual a %d\n", x, y );
	   }

	h) printf( "A soma é %d\n", x + y );

	i) printf( "O valor que você digitou é: %d\n" , valor );